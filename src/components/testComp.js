import React, {
    Component,
    PropTypes,
} from 'react';

class testComp extends Component {
    render() {
        return (
            <div></div>
        );
    }
}

testComp.propTypes = {};
testComp.defaultProps = {};

export default testComp;
